function SetActiveLink([list], index) {
  // const list = document.querySelectorAll("list");
  // list.forEach((item) => {
  //   item.classList.remove("active");
  // });
  // list[index].classList.add("active");
  console.log(list);
}

export default SetActiveLink;
